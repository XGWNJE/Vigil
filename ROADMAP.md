# Vigil 开发路线图

> 当前版本 v1.18.0（versionCode 24）。下一阶段尚未确定，新增目标需经 owner 确认后进入本路线图。

## 当前规划

暂无已确认的待开发里程碑。

## 已完成里程碑

| 阶段 | 交付版本 | 完成内容 | 验证摘要 |
|---|---|---|---|
| P0 | v1.9.0 | 设置 Sheet 增加上滑入口；权限按必需、推荐、可选分级；增加锁定任务卡片引导 | 2026-07-30 Motorola 真机验证通过 |
| P1 | v1.10.0 | 关键词级铃声与循环次数；报警自动结束与历史记录；进程重建后续播 | 2026-08-01 Motorola 真机闭环验证通过 |
| P2 | v1.11.0 | 铃声库支持导入音频、录音、重命名、删除和试听；文件失效时回落系统闹钟 | 2026-08-01 VisionGuard_API36 模拟器闭环验证通过 |
| P3 | v1.14.0 | 6 条内置预设语音；默认铃声和关键词级铃声均可直接选用 | 完整闭环、关键词级预设与进程恢复验证通过 |

## 后续已交付改进

- **v1.12.0 — 横屏适配**：解除竖屏锁定，修复横屏挖孔偏移、内容裁切和确认按钮不可达。
- **v1.13.0 — 监听断连自愈**：增加完整重连序列；自愈失败时提供立即重试和重新授权入口。
- **v1.15.0 — 缩短失效反馈**：断连自愈无效时约 10 秒内提示重新授权，避免长时间停留在“重连中”。
- **v1.16.0 — 循环次数与报警快捷通知**：循环次数调整为 1–10 次线性范围并取消无限循环；修复系统铃声无法可靠按次数结束；响铃期间可从高优先级前台通知快速进入处理。
- **v1.17.0 — 应用自动更新**：冷启动自动检查 GitHub Release，展示发版说明并支持下载、授权和调起系统安装；设置页支持手动检查。
- **v1.18.0 — 连续报警可靠性**：不同关键词 FIFO 排队、同词聚合与可配置重复提醒间隔；修复后台报警结束后回到前台仍显示旧弹窗。API 36 模拟器压力验证覆盖 5 条 FIFO、同词 10 次触发、冷却边界、强制崩溃恢复与资源释放；真机通过自动更新安装后继续补验。

各版本的完整用户可见变更以 `release-notes/` 为准；当前功能事实以 `README.md` 为准；实现约束、验证流程与平台兼容经验以 `AGENTS.md` 为准。
