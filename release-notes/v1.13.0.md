# v1.13.0

## 修复：监听断开后自动重连失效 —— 部分机型卡在"重连中"

**修复**
- 修复部分系统（小米澎湃 OS 等）上"关闭监听再打开后一直卡在重连中"的问题：之前仅依赖系统重新绑定接口，个别 ROM 会静默失败；升级为自动完整重建（停止 → 重置 → 重启监听服务），仍无法恢复时主屏会给出「立即重试 / 重新授权」引导，一键跳转系统设置重新授权，无需卸载重装或清除数据。
- 修复冷启动时短暂误显示"监听中"的问题（实际服务尚未连上）。
