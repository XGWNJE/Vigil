## 循环次数改为 1–10 次线性调节，所有铃声都能按次数可靠结束

**优化**
- 循环次数改为 1–10 次离散滑杆，取消无限循环和原有跳跃档位，提醒时长更直观可控。
- 旧版“直到确认”、50 次和 100 次设置会自动迁移到 10 次，无需重新配置关键词。

**修复**
- 修复部分系统默认闹钟自带循环标记时无法按次数自动结束的问题，达到设定次数后会可靠停铃、关闭弹窗并写入报警记录。
