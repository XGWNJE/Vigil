## 连续提醒不丢失，后台弹窗按真实报警状态收口

**新增**
- 不同关键词短时间连续出现时按触发顺序排队，当前报警结束后自动提醒下一条。
- 新增重复提醒间隔，可选择不忽略、30 秒、1 分钟、3 分钟、5 分钟或 10 分钟。

**优化**
- 同一来源的同一关键词连续出现时合并为一次报警并累计次数，减少重复轰炸。
- 当前报警与等待队列都会持久保存，应用进程恢复后继续按原顺序处理。

**修复**
- 修复应用在后台触发报警并自动结束后，重新回到前台仍显示旧报警弹窗的问题。
